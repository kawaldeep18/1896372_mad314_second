package com.example.student;

import java.util.ArrayList;
import java.util.List;

public class StudentDataSourcer {

    private static StudentDataSourcer instance=null;
    private List<student> students=new ArrayList<>();
    private StudentDataSourcer(){
       student Students=new student();
        student name="marc";
        student id=1;
        this.students.add(student);
  }
  public static  StudentDataSourcer getInstance(){
    if (instance=null){
        instance=new StudentDataSourcer();
    }
    return instance;
    public void addstudent(student students);
      {this.students.add(student);
      }studentList.add(student)


    }
}












//}
